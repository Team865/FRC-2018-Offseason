plugins {
    id "java"
    id "edu.wpi.first.GradleRIO" version "2018.06.21"
}

subprojects {
    project.ext {
        TEAM_NUMBER = 865
    }

    repositories {
        mavenCentral()
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '4.7'
}